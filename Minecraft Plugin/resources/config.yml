# Minecart 2.0.0
# Plugin Feito Pela SnowDev
# Site https://minecart.com.br

Minecart:
  ShopKey: 'minecart_shop_key_api'
  ShopServer: 'minecart_server_name'

config:
  force_clean_inventry: true
  prefix: '&b[Minecart] '

cmd:
  cmd_active_vip: 'darvip {player.name} {key.group} {key.duration}'
  cmd_active_cash: 'cash give {player.name} {cash.quantity}'